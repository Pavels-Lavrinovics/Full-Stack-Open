```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: GET request
    server-->>browser: Server returns HTML document.

    browser->>server: Browser asks for style sheet file
    server-->>browser: Server sends css file

    browser->>server: Browser asks for js file
    server-->>browser: Server sends js file

    browser->>server: Browser asks for json file
    server-->>browser: Server sends json file
```